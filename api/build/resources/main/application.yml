spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blogdb?allowPublicKeyRetrieval=true&sslMode=DISABLED&serverTimezone=Asia/Seoul&characterEncoding=utf8
    username: root
    password: 123qazasd!
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 50
server:
  port: 4000

app:
  jwt:
    secret: ${JWT_SECRET:skdkstlguddlsepduswnrlqnsEkrwhgek!}
    expiry-minutes: 60